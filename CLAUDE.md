@AGENTS.md

# Claude Code notes

- When a plan is approved in a Claude Code session (including plan-mode "save it"), its canonical copy is `docs/plans/active/<title>.md` in this repo, using the frontmatter in `docs/plans/templates/plan.md`, committed before implementation starts. The copy under `~/.claude/plans/` is a draft only — never the source of truth.
