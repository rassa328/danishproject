Context: The Danish visual identity is deliberately minimal; competing highlights and animations would dilute it.
Decision: The site uses exactly one accent color (Dannebrog red via the `--accent` token) and exactly one animated moment site-wide, firing on transition into an earned state.
Consequence: New features express emphasis through the existing accent token — no new accent colors, and no animation outside the single celebration moment.
